<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="me.deshark.lms.infrastructure.mapper.UserMapper">
    <insert id="insert">
        INSERT INTO users (uuid, username, password_hash, email, status)
        VALUES (
        #{uuid},
        #{username},
        #{passwordHash},
        #{email},
        #{status}
        )
    </insert>
</mapper>
